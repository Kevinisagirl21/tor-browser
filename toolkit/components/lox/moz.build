EXTRA_JS_MODULES += [
    "Lox.sys.mjs",
    # Let's keep the old jsm format until wasm-bindgen is updated
    "lox_wasm.jsm",
]

JAR_MANIFESTS += ["jar.mn"]

XPIDL_MODULE = "lox"
XPIDL_SOURCES += ["ILoxService.idl"]
XPCOM_MANIFESTS += [
    "components.conf",
]

EXPORTS.torproject += ["LoxService.h"]
